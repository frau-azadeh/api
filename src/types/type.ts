export type PostPage = {
    id: number;
    title: string;
    body: string;
}

export type PhotoPage = {
   id:number;
   author:string;
   download_url:string;
}

export type TodoPage = {
    id: number;
    title: string;
    completed: string;
}